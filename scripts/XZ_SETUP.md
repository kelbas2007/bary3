# Настройка xz для Windows

## ✅ Установка xz

xz для Windows можно скачать с официального сайта: https://tukaani.org/xz/

После распаковки архива `xz-5.8.2-windows` структура выглядит так:
```
xz-5.8.2-windows/
├── bin_x86-64/
│   ├── xz.exe          ← Основной исполняемый файл
│   ├── liblzma.dll     ← Библиотека
│   └── ...
├── bin_i686-sse2/
├── doc/
└── include/
```

## 🔧 Автоматическое обнаружение

Скрипты автоматически ищут xz в следующих местах (в порядке приоритета):

1. **PATH** - если xz добавлен в системный PATH
2. **Downloads** - `%USERPROFILE%\Downloads\xz-5.8.2-windows\bin_x86-64\xz.exe`
3. **Program Files** - `C:\Program Files\xz\bin\xz.exe`

## 📝 Добавление в PATH (опционально)

Для удобства можно добавить xz в PATH:

1. Скопировать `bin_x86-64` в постоянное место (например, `C:\Tools\xz`)
2. Добавить путь в системный PATH:
   - Win + R → `sysdm.cpl` → Вкладка "Дополнительно" → "Переменные среды"
   - В "Системные переменные" найти `Path` → "Изменить"
   - Добавить путь к `bin_x86-64` (например, `C:\Tools\xz\bin_x86-64`)

**Примечание:** Это не обязательно - скрипты найдут xz автоматически в Downloads.

## ✅ Проверка установки

Проверить, что xz найден:
```powershell
cd C:\flutter_projects\bary3
.\scripts\compress_model_smart.ps1
```

Скрипт должен показать:
```
✅ Found native xz: C:\Users\...\Downloads\xz-5.8.2-windows\bin_x86-64\xz.exe
```

## 🚀 Использование

После установки xz скрипты автоматически используют его для сжатия с максимальными параметрами:
- `-9` - максимальный уровень сжатия
- `-e` - extreme mode (лучшее сжатие, медленнее)

Это должно дать лучшие результаты сжатия по сравнению с 7-Zip.
