# РУКОВОДСТВО ПО ЛОКАЛИЗАЦИИ

## Обзор

Приложение Bary3 поддерживает 3 языка:
- 🇷🇺 Русский (ru) - основной
- 🇬🇧 Английский (en)
- 🇩🇪 Немецкий (de)

Локализация реализована через Flutter Intl с использованием ARB файлов.

---

## Структура локализации

### Файлы локализации

```
lib/l10n/
├── app_ru.arb          # Русский (основной, template)
├── app_en.arb          # Английский
├── app_de.arb          # Немецкий
├── app_localizations.dart      # Базовый класс (генерируется)
├── app_localizations_ru.dart  # Реализация для ru (генерируется)
├── app_localizations_en.dart  # Реализация для en (генерируется)
└── app_localizations_de.dart  # Реализация для de (генерируется)
```

### Конфигурация

Файл `l10n.yaml`:
```yaml
arb-dir: lib/l10n
template-arb-file: app_ru.arb
output-localization-file: app_localizations.dart
untranslated-messages-file: untranslated_messages.txt
```

---

## Как добавить новый ключ локализации

### Шаг 1: Добавить ключ в app_ru.arb

Откройте `lib/l10n/app_ru.arb` и добавьте новый ключ:

```json
{
  "myNewKey": "Мой новый текст",
  "@myNewKey": {
    "description": "Описание ключа"
  }
}
```

### Шаг 2: Добавить переводы в другие языки

**app_en.arb:**
```json
{
  "myNewKey": "My new text"
}
```

**app_de.arb:**
```json
{
  "myNewKey": "Mein neuer Text"
}
```

### Шаг 3: Добавить параметры (если нужно)

Для строк с параметрами:

**app_ru.arb:**
```json
{
  "welcomeMessage": "Привет, {name}!",
  "@welcomeMessage": {
    "placeholders": {
      "name": {
        "type": "String"
      }
    }
  }
}
```

**app_en.arb:**
```json
{
  "welcomeMessage": "Hello, {name}!"
}
```

### Шаг 4: Сгенерировать файлы локализации

```bash
flutter gen-l10n
```

### Шаг 5: Использовать в коде

```dart
import '../l10n/app_localizations.dart';

// В методе build или другом месте с доступом к context
final l10n = AppLocalizations.of(context)!;

// Простая строка
Text(l10n.myNewKey)

// С параметрами
Text(l10n.welcomeMessage('Иван'))
```

---

## Соглашения по именованию ключей

### Структура: `модуль_компонент_элемент`

Примеры:
- `balance_currentBalance` - баланс, текущий баланс
- `piggyBanks_createSheet_title` - копилки, форма создания, заголовок
- `calendar_eventCompleted` - календарь, событие выполнено
- `settings_language` - настройки, язык

### Префиксы по модулям:

- `common_*` - общие элементы (кнопки, действия)
- `balance_*` - экран баланса
- `piggyBanks_*` - копилки
- `calendar_*` - календарь
- `settings_*` - настройки
- `bariChat_*` - чат с Бари
- `calculators_*` - калькуляторы
- `earningsLab_*` - лаборатория заработка
- `minitrainers_*` - мини-тренажёры
- `exportImport_*` - экспорт/импорт
- `parentZone_*` - родительская зона

---

## Замена хардкодных строк

### ❌ Неправильно:

```dart
Text('Привет, мир!')
const Text('Кнопка')
SnackBar(content: Text('Ошибка'))
```

### ✅ Правильно:

```dart
final l10n = AppLocalizations.of(context)!;
Text(l10n.common_hello)
Text(l10n.common_button)
SnackBar(content: Text(l10n.common_error))
```

---

## Параметры в строках

### Простые параметры:

**ARB:**
```json
{
  "greeting": "Привет, {name}!",
  "@greeting": {
    "placeholders": {
      "name": {"type": "String"}
    }
  }
}
```

**Код:**
```dart
Text(l10n.greeting('Иван'))
```

### Множественные параметры:

**ARB:**
```json
{
  "summary": "Сумма: {amount}, Дата: {date}",
  "@summary": {
    "placeholders": {
      "amount": {"type": "String"},
      "date": {"type": "String"}
    }
  }
}
```

**Код:**
```dart
Text(l10n.summary('100₽', '01.01.2024'))
```

### Плюрализация:

**ARB:**
```json
{
  "itemsCount": "{count,plural, =1{1 предмет} few{{count} предмета} many{{count} предметов} other{{count} предметов}}",
  "@itemsCount": {
    "placeholders": {
      "count": {"type": "int"}
    }
  }
}
```

**Код:**
```dart
Text(l10n.itemsCount(5))  // "5 предметов"
Text(l10n.itemsCount(1))  // "1 предмет"
```

---

## Проверка локализации

### Автоматическая проверка

Используйте скрипт для поиска хардкодных строк:

```bash
dart scripts/check_hardcoded_strings.dart
```

### Ручная проверка

1. Запустите приложение
2. Переключите язык в настройках
3. Проверьте все экраны на наличие непереведённых строк

---

## Частые ошибки

### ❌ Ошибка: Использование const с локализацией

```dart
// Неправильно
const Text(l10n.someKey)  // Ошибка!

// Правильно
Text(l10n.someKey)
```

### ❌ Ошибка: Забыли добавить l10n в метод

```dart
// Неправильно
Widget build(BuildContext context) {
  return Text(l10n.someKey);  // l10n не определён!
}

// Правильно
Widget build(BuildContext context) {
  final l10n = AppLocalizations.of(context)!;
  return Text(l10n.someKey);
}
```

### ❌ Ошибка: Не добавили ключ во все языки

Всегда добавляйте ключ во все 3 ARB файла (ru, en, de)!

---

## Лучшие практики

1. **Всегда используйте локализацию** для пользовательских строк
2. **Не используйте const** с локализованными строками
3. **Добавляйте описания** к ключам в ARB файлах
4. **Проверяйте все языки** после добавления новых ключей
5. **Используйте осмысленные имена** ключей
6. **Группируйте ключи** по модулям/экранам
7. **Не дублируйте** одинаковые строки - используйте общие ключи (common_*)

---

## Инструменты

### Скрипт проверки хардкодных строк

```bash
dart scripts/check_hardcoded_strings.dart
```

### Генерация файлов локализации

```bash
flutter gen-l10n
```

### Проверка непереведённых сообщений

После генерации проверьте файл `untranslated_messages.txt`

---

## Статистика

- **Всего ключей:** ~850+
- **Поддерживаемых языков:** 3
- **Файлов с локализацией:** Все экраны и виджеты

---

## Полезные ссылки

- [Flutter Internationalization](https://docs.flutter.dev/development/accessibility-and-localization/internationalization)
- [ARB Format Specification](https://github.com/google/app-resource-bundle)

---

**Последнее обновление:** Декабрь 2024
