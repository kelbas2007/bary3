# ФУНКЦИЯ: АВТОМАТИЧЕСКОЕ ВЫПОЛНЕНИЕ ЗАПЛАНИРОВАННЫХ СОБЫТИЙ

**Дата:** Декабрь 2024  
**Статус:** ✅ Реализовано

---

## 📋 ЗАДАЧА

При планировании запланированного действия спрашивать пользователя и добавить механику автоматически вычитать или добавлять сумму при наступлении даты события.

---

## ✅ РЕАЛИЗОВАННЫЕ ИЗМЕНЕНИЯ

### 1. **Модель PlannedEvent** (`lib/models/planned_event.dart`)

Добавлено новое поле `autoExecute`:
- Тип: `bool`
- По умолчанию: `false`
- Описание: Автоматически выполнять событие при наступлении даты

**Изменения:**
- Добавлено поле в конструктор
- Добавлено в `toJson()` и `fromJson()` для сериализации
- Добавлено в `copyWith()` для копирования

---

### 2. **Экран планирования событий** (`lib/screens/plan_event_screen.dart`)

Добавлен чекбокс для выбора автоматического выполнения:

**UI:**
```dart
SwitchListTile(
  title: const Text(
    'Автоматически выполнить при наступлении даты',
    style: TextStyle(color: Colors.white),
  ),
  subtitle: const Text(
    'Сумма будет автоматически добавлена или вычтена из баланса',
    style: TextStyle(color: Colors.white70, fontSize: 12),
  ),
  value: _autoExecute,
  onChanged: (value) {
    setState(() {
      _autoExecute = value;
    });
  },
),
```

**Логика:**
- При создании/редактировании события сохраняется значение `autoExecute`
- При создании повторяющихся событий значение `autoExecute` передается всем производным событиям

---

### 3. **Экран календаря** (`lib/screens/calendar_screen.dart`)

Добавлена логика автоматического выполнения событий:

#### Метод `_checkMissedEvents()`:
- Проверяет все запланированные события, дата которых уже прошла
- Если `autoExecute == true`: автоматически выполняет событие
- Если `autoExecute == false` и событие не повторяющееся: помечает как пропущенное

#### Метод `_autoExecuteEvent()`:
- Создает транзакцию автоматически с суммой события
- Обновляет статус события на `completed`
- Если событие повторяющееся, создает следующее событие с сохранением `autoExecute`
- Начисляет XP (+15)
- Добавляет действие в память Бари

#### Метод `_createNextRepeatEventForAutoExecute()`:
- Создает следующее повторяющееся событие
- Сохраняет настройку `autoExecute` для следующего события
- Планирует уведомление для следующего события

---

## 🔄 ЛОГИКА РАБОТЫ

### Сценарий 1: Событие с автоматическим выполнением

1. Пользователь создает событие с включенным `autoExecute`
2. При наступлении даты события:
   - Создается транзакция автоматически
   - Сумма добавляется/вычитается из баланса
   - Событие помечается как выполненное
   - Если событие повторяющееся, создается следующее с тем же `autoExecute`
   - Начисляется XP
   - Добавляется действие в память Бари

### Сценарий 2: Событие без автоматического выполнения

1. Пользователь создает событие с выключенным `autoExecute`
2. При наступлении даты события:
   - Если событие не повторяющееся, помечается как пропущенное
   - Пользователь может выполнить его вручную позже

### Сценарий 3: Повторяющееся событие с автоматическим выполнением

1. Пользователь создает повторяющееся событие (например, еженедельное) с `autoExecute = true`
2. При наступлении каждой даты:
   - Событие выполняется автоматически
   - Создается следующее событие с тем же `autoExecute`
   - Процесс повторяется для всех последующих дат

---

## 📊 ПРЕИМУЩЕСТВА

1. **Автоматизация**: Пользователю не нужно вручную выполнять регулярные события
2. **Гибкость**: Пользователь может выбрать, какие события выполнять автоматически
3. **Удобство**: Особенно полезно для регулярных доходов/расходов (зарплата, подписки и т.д.)
4. **Сохранение настроек**: Для повторяющихся событий настройка `autoExecute` сохраняется

---

## 🧪 ТЕСТИРОВАНИЕ

- ✅ Код проходит анализ без ошибок
- ✅ Нет предупреждений линтера
- ✅ Правильная сериализация/десериализация поля `autoExecute`
- ✅ Правильная передача `autoExecute` в повторяющиеся события

---

## 📝 ФАЙЛЫ

**Изменены:**
- `lib/models/planned_event.dart` - добавлено поле `autoExecute`
- `lib/screens/plan_event_screen.dart` - добавлен чекбокс и логика сохранения
- `lib/screens/calendar_screen.dart` - добавлена логика автоматического выполнения

**Статус:** ✅ **РЕАЛИЗОВАНО**

