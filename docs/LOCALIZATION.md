# Руководство по локализации

Руководство по добавлению новых языков, работе с ARB файлами и лучшим практикам локализации.

## Обзор

Приложение Bary3 поддерживает 3 языка:
- 🇷🇺 Русский (ru) - основной
- 🇬🇧 Английский (en)
- 🇩🇪 Немецкий (de)

Локализация реализована через Flutter Intl с использованием ARB файлов.

## Структура локализации

### Файлы локализации

```
lib/l10n/
├── app_ru.arb          # Русский (основной, template)
├── app_en.arb          # Английский
├── app_de.arb          # Немецкий
├── app_localizations.dart      # Базовый класс (генерируется)
├── app_localizations_ru.dart  # Реализация для ru (генерируется)
├── app_localizations_en.dart  # Реализация для en (генерируется)
└── app_localizations_de.dart  # Реализация для de (генерируется)
```

### Конфигурация

Файл `l10n.yaml`:
```yaml
arb-dir: lib/l10n
template-arb-file: app_ru.arb
output-localization-file: app_localizations.dart
untranslated-messages-file: untranslated_messages.txt
```

## Как добавить новый ключ локализации

### Шаг 1: Добавить ключ в app_ru.arb

Откройте `lib/l10n/app_ru.arb` и добавьте новый ключ:

```json
{
  "myNewKey": "Мой новый текст",
  "@myNewKey": {
    "description": "Описание ключа"
  }
}
```

### Шаг 2: Добавить переводы в другие языки

**app_en.arb:**
```json
{
  "myNewKey": "My new text"
}
```

**app_de.arb:**
```json
{
  "myNewKey": "Mein neuer Text"
}
```

### Шаг 3: Сгенерировать локализацию

```bash
flutter gen-l10n
```

### Шаг 4: Использовать в коде

```dart
final l10n = AppLocalizations.of(context)!;
Text(l10n.myNewKey)
```

## Параметризованные строки

### С одним параметром

**app_ru.arb:**
```json
{
  "greeting": "Привет, {name}!",
  "@greeting": {
    "placeholders": {
      "name": {
        "type": "String"
      }
    }
  }
}
```

**Использование:**
```dart
Text(l10n.greeting('Иван'))
```

### С несколькими параметрами

**app_ru.arb:**
```json
{
  "transactionCount": "У вас {count} транзакций за {period}",
  "@transactionCount": {
    "placeholders": {
      "count": {
        "type": "int"
      },
      "period": {
        "type": "String"
      }
    }
  }
}
```

**Использование:**
```dart
Text(l10n.transactionCount(5, 'неделю'))
```

## Локализация без BuildContext

Для сервисов без доступа к `BuildContext` (например, `NotificationService`):

```dart
static String _getLocalizedString(String key, String locale) {
  final localeMap = {
    'ru': {
      'key1': 'Текст на русском',
      'key2': 'Другой текст',
    },
    'en': {
      'key1': 'English text',
      'key2': 'Another text',
    },
    'de': {
      'key1': 'Deutscher Text',
      'key2': 'Anderer Text',
    },
  };
  final lang = locale.startsWith('ru') ? 'ru' 
             : locale.startsWith('en') ? 'en' 
             : locale.startsWith('de') ? 'de' 
             : 'ru';
  return localeMap[lang]?[key] ?? localeMap['ru']![key]!;
}
```

## Лучшие практики

### 1. Всегда используйте локализацию

❌ **Плохо:**
```dart
Text('Привет')
```

✅ **Хорошо:**
```dart
Text(l10n.greeting)
```

### 2. Используйте описательные ключи

❌ **Плохо:**
```json
{
  "key1": "Текст"
}
```

✅ **Хорошо:**
```json
{
  "balance_screen_title": "Баланс",
  "@balance_screen_title": {
    "description": "Заголовок экрана баланса"
  }
}
```

### 3. Группируйте ключи по экранам/модулям

```json
{
  "balance_title": "...",
  "balance_addIncome": "...",
  "balance_addExpense": "...",
  "piggyBanks_title": "...",
  "piggyBanks_create": "..."
}
```

### 4. Проверяйте все языки

После добавления нового ключа:
1. Добавьте переводы во все языки
2. Запустите `flutter gen-l10n`
3. Проверьте, что нет ошибок
4. Протестируйте на всех языках

## Добавление нового языка

### Шаг 1: Создать ARB файл

Создайте `lib/l10n/app_xx.arb` (где xx - код языка)

### Шаг 2: Скопировать структуру

Скопируйте все ключи из `app_ru.arb` и переведите их

### Шаг 3: Обновить конфигурацию

В `pubspec.yaml` добавьте поддержку языка (если нужно)

### Шаг 4: Сгенерировать

```bash
flutter gen-l10n
```

## Проверка локализации

### Проверка отсутствующих переводов

```bash
flutter gen-l10n
# Проверьте файл untranslated_messages.txt
```

### Тестирование локализации

1. Запустите приложение
2. Переключите язык в настройках
3. Проверьте все экраны на правильную локализацию
4. Проверьте уведомления
5. Проверьте AI ответы

## Статус локализации

- ✅ Русский (ru) - 100% покрытие
- ✅ Английский (en) - 100% покрытие
- ✅ Немецкий (de) - 100% покрытие

Все новые функции локализованы на все языки.

---

*Последнее обновление: Январь 2025*
